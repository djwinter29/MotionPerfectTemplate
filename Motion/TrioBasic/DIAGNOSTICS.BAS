
WHILE TRUE

    vr_liner_motor_speed = SPEED AXIS(axis_liner)
    vr_liner_motor_pos = MPOS AXIS(axis_liner)
    vr_rotate_motor_speed = SPEED AXIS(axis_rotate)
    vr_rotate_motor_pos = MPOS AXIS(axis_rotate)

    WA(1)
WEND
